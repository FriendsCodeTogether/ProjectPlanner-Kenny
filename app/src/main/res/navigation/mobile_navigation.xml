<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/mobile_navigation"
    app:startDestination="@+id/nav_home">

    <fragment
        android:id="@+id/nav_home"
        android:name="com.mobileapps2.projectplanner.ui.home.HomeFragment"
        android:label="@string/menu_home"
        tools:layout="@layout/fragment_home" >
        <action
            android:id="@+id/action_nav_home_to_boardActivity"
            app:destination="@id/boardActivity" />
    </fragment>
    <activity
        android:id="@+id/boardActivity"
        android:name="com.mobileapps2.projectplanner.ui.boards.BoardActivity"
        android:label="activity_board"
        tools:layout="@layout/activity_board" />

</navigation>